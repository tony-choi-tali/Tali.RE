import { Box } from "@mui/material";
import { projectName } from "../../theme/themeConfig";

const Footer = () => {
  return <Box className="footer">{projectName} @ DEVELO IT</Box>;
};

export default Footer;
